/**
 * Client-side authentication functions
 * These functions run in the browser and handle user authentication flows
 */

export { login } from "./login";
export { register } from "./register";
export { logout } from "./logout";
